{
  "kind": "collectionType",
  "collectionName": "regions",
  "info": {
    "name": "Region"
  },
  "options": {
    "increments": true,
    "timestamps": true
  },
  "attributes": {
    "name": {
      "type": "string",
      "required": true
    },
    "cities": {
      "via": "region",
      "collection": "city"
    }
  }
}
